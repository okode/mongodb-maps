version: '2'

services:

  app:
    container_name: mongodb-maps-app
    image: okode/mongodb-maps
    build: app
    links:
      - db
    ports:
      - "8080:8080"

  db:
    container_name: mongodb-maps-db
    image: mongo
    ports:
      - "27017:27017"
